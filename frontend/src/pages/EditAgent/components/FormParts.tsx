/**
 * Re-export the shared design-system primitives so existing imports
 * from '@/pages/EditAgent/components/FormParts' keep working.
 * The canonical implementations live in '@/components/ui'.
 */
export { FormCard } from '@/components/ui/FormCard';
export { FormRow } from '@/components/ui/FormRow';
