"""Business-logic services & external clients used by the API layer.

Holds infrastructure that is NOT request handling (routers) nor persistence
(models), e.g. OSS upload, MCP JWT signing, skill-zip parsing.
"""
